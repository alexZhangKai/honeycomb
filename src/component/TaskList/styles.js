// @flow

import {
    StyleSheet
} from 'react-native';

export default StyleSheet.create({
    container: {
        backgroundColor: 'white'
    },
    headline: {
        flexDirection: 'row',
        justifyContent: 'space-between'
    }
});
