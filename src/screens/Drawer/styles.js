// @flow

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    drawer: {
        flex: 1,
        width: 300,
        alignItems: 'center',
        justifyContent: 'center'
    }
});
